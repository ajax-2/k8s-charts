apiVersion: v1
kind: Service
metadata:
  name: {{ template "seata.fullname" . }}
  labels:
    app: seata
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: seata
